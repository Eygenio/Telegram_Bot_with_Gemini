# 🤖 Telegram Gemini Bot — Ассистент с памятью диалогов

Интеллектуальный Telegram-бот, использующий **Google Gemini API** с поддержкой контекстных ответов. Бот ведёт полноценные диалоги и запоминает историю сообщений пользователя.

---

## ✨ Особенности
- 💬 **Контекстные диалоги** (передача истории сообщений в Gemini)
- 🔄 **Перезапуск диалога** по кнопке «🆕 Новый запрос»
- 🧠 **Сохранение всех сообщений** в SQLite
- ⚡ Быстрая работа на `aiogram 3`
- 🐳 Готовая сборка через **Docker** и `docker-compose`
- 🔐 Поддержка `.env` и безопасного хранения ключей
- 🗂️ Простая и модульная архитектура

---

## 🏗️ Архитектура проекта
Файлы проекта:
```
project/
 ├── bot.py
 ├── gemini_client.py
 ├── config.py
 ├── database.py
 ├── .env
 ├── requirements.txt
 ├── Dockerfile
 ├── docker-compose.yml
 └── README.md
```

Используемые технологии:
- **Aiogram 3.4** — современный Telegram-фреймворк
- **Google Gemini API (google-genai)** — генерация текста
- **SQLite** — база данных для хранения пользователей и истории
- **Docker + docker-compose** — деплой и автоматический перезапуск

---

## 💡 Функциональность
### 👤 Для пользователей
- Отправка текстовых сообщений
- Ответы от модели Gemini 2.5 Flash
- Автоматическое сохранение контекста
- Возможность начать новый диалог
- Удобная клавиатура

### 🛠️ Для разработчиков
- Простой и чистый код
- Контекст формируется локально
- Логирование сообщений в SQLite
- Минимум зависимостей
- Полностью готов для продакшена

---

## 🚀 Запуск проекта (локально)

### 1. Клонировать репозиторий
```bash
git clone https://github.com/Eygenio/Telegram_Bot_with_Gemini
cd Telegram_Bot_with_Gemini
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Создать файл `.env`
```env
TELEGRAM_TOKEN=ваш_telegram_токен
GEMINI_API_KEY=ваш_gemini_ключ
GEMINI_MODEL=gemini-2.5-flash
```

### 4. Запустить
```bash
python bot.py
```

---

## 🐳 Запуск через Docker

### 1. Собрать контейнер
```bash
docker-compose build
```

### 2. Запустить
```bash
docker-compose up -d
```

### 3. Смотреть логи
```bash
docker-compose logs -f
```

---

## 📦 Структура данных в SQLite

### Таблица `users`
| user_id |
|---------|

### Таблица `messages`
| id | user_id | role | content | timestamp |

Роли:
- `user` — сообщение пользователя
- `model` — ответ Gemini

---

## ⚙️ Использование Gemini API
Пример обращения:
```python
response = client.models.generate_content(
    model="gemini-2.5-flash",
    contents=[
        {"role": "user", "parts": [{"text": "Привет"}]},
        {"role": "model", "parts": [{"text": "Привет! Как я могу помочь?"}]}
    ]
)
```

Получение текста:
```python
response.text
```

---

## 🔐 Безопасность
- Токены хранятся только в `.env`
- Ключи не пишутся в логи
- SQLite изолирована внутри Docker-контейнера
- Бот автоматически перезапускается при сбоях

---

Если нужно — могу:
- добавить раздел «Установка на сервер»
- оформить changelog
- сделать красивый shields.io header
- добавить примеры API-ответов
- включить картинки/диаграммы архитектуры


---
